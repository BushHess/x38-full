# X38 Governance Audit — 2026-03-24

**Scope**: `/var/www/trading-bots/btc-spot-dev/research/x38`  
**Audit mode**: lead investigation + independent discoverer + independent challenger + authority/citation audit  
**Prior audit comparison**: 2 `RESOLVED`, 0 `PERSISTING`, 0 `REGRESSION`, 4 `NEW`

Resolved since the earlier 2026-03-24 audit:
- The open-topic `Cross-topic tensions` backfill is now present across the active topic dossiers.
- Topic `004` now uses canonical `Judgment call` vocabulary in its §14 closure rows.

## [WARNING][NEW] `debate-index.md` gives two incompatible answers about whether Topic 016 is immediately debateable after Wave 1

What is wrong: the authoritative wave section first says that, after Topic `007` closes, all remaining topics except `003` and `014` may debate in parallel (`debate/debate-index.md:54-56`). The same authoritative file then places Topic `016` in Wave 2.5 with hard prerequisites (`debate/debate-index.md:61-69`, `debate/debate-index.md:113-116`), and Topic `016`'s own README agrees that it stays backlog-only until `001 + 002 + 010 + 011 + 015` close (`debate/016-bounded-recalibration-path/README.md:5`, `debate/016-bounded-recalibration-path/README.md:68-83`).

Authoritative basis: the audit contract says `debate-index.md` is authoritative for waves and dependencies; the document should not internally overstate Wave 2 concurrency.

Workflow impact: this is present-tense readiness drift, not a live block. The stricter Wave 2.5/backlog clauses still exist, so `016` has not actually been opened out of order. But the broad “all remaining topics” sentence can misroute Wave planning and make `003` look closer to unblocked than it is.

What should be synced: narrow the Wave 2 prose so `016` is explicitly excluded from the immediate post-007 parallel set.

Verification note: independently surfaced by the authority audit; challenger agreed the real issue is the internal contradiction in the authoritative index, not a repo-wide dependency collapse.

## [WARNING][NEW] Workflow status ledgers are stale after Wave 1 closure and draft seeding

What is wrong: `EXECUTION_PLAN.md` still says “6 rounds done” at the top (`EXECUTION_PLAN.md:3-4`) while the current-status table records `16` rounds (`EXECUTION_PLAN.md:28`). The same file still says Phase 2 is `CHƯA BẮT ĐẦU` (`EXECUTION_PLAN.md:168-170`) even though it immediately documents Topic `007` closed, Wave 2 unblocked, and Topic `001` closed inside the Wave 2 table (`EXECUTION_PLAN.md:176-182`, `EXECUTION_PLAN.md:193`). It also still says `Specs drafted | ZERO` (`EXECUTION_PLAN.md:29`) while the drafts layer already contains a seeded `architecture_spec.md` and `drafts/README.md` records that seeded state (`drafts/README.md:10`, `drafts/architecture_spec.md:3-6`). `PLAN.md` likewise still says `Drafts | NOT STARTED` (`PLAN.md:1073`) despite the seeded draft.

Authoritative basis: these files are mandatory workflow context in bootstrap (`AGENTS.md:15-20`) and are explicitly meant to tell agents “mình đang ở đâu trong quy trình” (`EXECUTION_PLAN.md:3-5`).

Workflow impact: non-blocking but material. The higher-authority topic dirs and `debate-index.md` still let a careful reader recover the correct state, but the status ledgers can still misstate the current phase, next action, and whether drafting has begun.

What should be synced: update round totals, Phase 2 status, the “next step” section, and the draft counters in `PLAN.md` / `EXECUTION_PLAN.md` so they match the current repository state.

Verification note: challenger downgraded the broader “PLAN/index drift” hypothesis; the surviving claim is narrower and file-backed.

## [WARNING][NEW] Four future-wave dossiers are not yet debate-ready from an evidence-pointer standpoint

Targeted denominator: 4 topic dossiers inspected because they gate later-wave work or cross-topic routing: `003`, `010`, `014`, `016`.

What is wrong: all 4 still contain at least one outside-x38 evidence bullet that is not reconstructible as a concrete file:line pointer, or lacks the required `[extra-archive]` label.

- Topic `003` cites `V8 spec_1` and `V8 SPEC_REQUEST_PROMPT` as evidence without a concrete path or line (`debate/003-protocol-engine/findings-under-review.md:45-51`).
- Topic `010` cites `btc-spot-dev validation pipeline` / `btc-spot-dev validation WFO` as precedent without a file pointer (`debate/010-clean-oos-certification/findings-under-review.md:116-123`, `debate/010-clean-oos-certification/findings-under-review.md:205-209`).
- Topic `014` cites outside material as `V6`, `V8`, and `v10 backtest engine` without `[extra-archive]` labeling on those bullets (`debate/014-execution-resilience/findings-under-review.md:65-70`), even though §18 requires that label for evidence outside `research/x38/` (`debate/rules.md:93-97`).
- Topic `016` cites `Trail sweep evidence` and `X22 (Cost Sensitivity)` as outside evidence without a concrete file pointer (`debate/016-bounded-recalibration-path/findings-under-review.md:89-98`).

Authoritative basis: debate claims must have a verifiable evidence pointer (`debate/rules.md:10-13`), and outside-x38 evidence pointers must carry `[extra-archive]` (`debate/rules.md:93-97`).

Workflow impact: warning, not `[BLOCK-DEBATE]`, because all four dossiers are still round-0 seed artifacts and no round has started from them yet. But they are not cleanly ready for adversarial review or blind reconstruction until the evidence bullets are normalized.

What should be synced: replace vague outside references with exact file paths and lines plus `[extra-archive]`, or demote them from “Evidence” to open questions/hypotheses.

Verification note: challenger kept this finding only at pre-debate hygiene severity; no closure artifact was shown to depend on these weak pointers yet.

## [NOTE][NEW] Lower-authority parallel summary docs still advertise superseded topic maps or statuses

What is wrong:
- The split summary in `debate/000-framework-proposal/findings-under-review.md` still marks findings routed to closed child topics `001` and `007` as `Open` (`debate/000-framework-proposal/findings-under-review.md:46-72`), even though those topics are closed in the live registry and final resolutions (`debate/debate-index.md:16`, `debate/debate-index.md:22`, `debate/001-campaign-model/final-resolution.md:15-16`, `debate/007-philosophy-mission/final-resolution.md:29`).
- `docs/validation_mapping_table.md` still proposes obsolete cluster routing such as `X38-T-13`, `X38-T-14a/b`, `X38-T-15` with reused `F-30`-`F-39` numbering (`docs/validation_mapping_table.md:150-183`, `docs/validation_mapping_table.md:408-409`), while the live registry already defines `013 = convergence`, `014 = execution`, `015 = artifact/versioning`, `016 = bounded recalibration` (`debate/debate-index.md:28-31`).

Authoritative basis: topic dirs outrank ordinary docs, and `PLAN.md` is subordinate to debate findings (`x38_RULES.md:84-94`). `docs/validation_mapping_table.md` itself also self-describes as a pre-debate reference, not authority (`docs/validation_mapping_table.md:3-8`).

Workflow impact: informational rather than blocking, because the higher-authority path still resolves the truth. The risk is mis-scoping or duplicate work if someone consults these parallel summaries instead of the live registry.

What should be synced: either annotate both docs as superseded snapshots or update them to mirror the current topic map and closure state.

Verification note: surfaced by the independent challenger as the main lower-authority trap after stronger candidate claims were downgraded.

## Resolved Meta-Issues

### Draft-seeding blocker claim was tested and dropped

The strongest initial blocker candidate was that `drafts/architecture_spec.md` had been created too early. After authority audit, that claim did not survive as a reportable governance finding: higher-authority workflow text allows editable drafts during debate and keeps publication gated on all dependencies (`x38_RULES.md:71-82`, `drafts/architecture_spec.md:3-6`). The stricter sentence in `drafts/README.md:3` is real, but it is not strong enough on its own to prove a present-tense `[BLOCK-DRAFT]` violation.

### Finding-ID drift claim was tested and dropped

The mixed use of legacy `F-*` headings and topic-local issue IDs (`X38-CA-*`, `X38-ER-*`, `X38-BR-*`) in Topics `013/014/016` looked like a registry defect at first pass. Challenge and authority audit showed that the governed field is the `issue_id`, and those IDs are internally reconstructable (`debate/rules.md:132-137`, `debate/013-convergence-analysis/findings-under-review.md:19`, `debate/014-execution-resilience/findings-under-review.md:15`, `debate/016-bounded-recalibration-path/findings-under-review.md:24`, `debate/016-bounded-recalibration-path/findings-under-review.md:113`). I therefore did not publish that claim as a governance finding.

## Methodological Limitations

- The same lead actor performed initial discovery and final adjudication. I reduced that risk with one independent discoverer, one independent challenger, and one authority/citation auditor, but self-adjudication still exists at the top level.
- I did not elevate any `[BLOCK-*]` finding, so no blind-reproduction pass was required for publication-standard blocker claims.

## Sync Table

| file | issue | fix | source |
|------|-------|-----|--------|
| `debate/debate-index.md` | Wave 2 prose overstates immediate parallel set and conflicts with Topic 016's Wave 2.5 gate | Rewrite `Wave 2` summary so `016` is explicitly excluded until its hard prerequisites close | `debate/debate-index.md:54-63`, `debate/debate-index.md:113-116` |
| `EXECUTION_PLAN.md` | Round totals, phase status, and next-step bullets are stale | Sync header, current-status table, Phase 2 state, and “Hành động tiếp theo” to post-001/post-007 reality | `EXECUTION_PLAN.md:3-4`, `EXECUTION_PLAN.md:28-29`, `EXECUTION_PLAN.md:168-182`, `EXECUTION_PLAN.md:321-361` |
| `PLAN.md` | Draft state still says `NOT STARTED` despite seeded draft | Update status/logistics to reflect seeded `architecture_spec.md` without implying publication readiness | `PLAN.md:1073`, `drafts/architecture_spec.md:3-6` |
| `debate/003-protocol-engine/findings-under-review.md` | Outside evidence bullets are not reconstructible as file:line pointers | Replace `V8 spec_1` / `V8 SPEC_REQUEST_PROMPT` shorthand with exact paths + lines + `[extra-archive]` | `debate/rules.md:10-13`, `debate/rules.md:93-97` |
| `debate/010-clean-oos-certification/findings-under-review.md` | `btc-spot-dev validation pipeline/WFO` precedent is not tied to a concrete outside file | Add explicit file:path:line pointers or demote the precedent from `Evidence` | `debate/rules.md:10-13`, `debate/rules.md:93-97` |
| `debate/014-execution-resilience/findings-under-review.md` | Outside evidence bullets lack required `[extra-archive]` labeling and exact pointers | Label the outside bullets and anchor them to concrete files/lines | `debate/rules.md:93-97` |
| `debate/016-bounded-recalibration-path/findings-under-review.md` | `Trail sweep evidence` / `X22` are vague extra-archive placeholders | Replace with exact outside file:line citations or move them to open-question prose | `debate/rules.md:10-13`, `debate/rules.md:93-97` |
| `debate/000-framework-proposal/findings-under-review.md` | Split summary still shows 001/007-routed findings as open | Mark transferred findings consistently with child-topic closure state, or add a superseded-summary note | `debate/001-campaign-model/final-resolution.md:15-16`, `debate/007-philosophy-mission/final-resolution.md:29` |
| `docs/validation_mapping_table.md` | Parallel cluster plan reuses superseded topic numbers and finding slots | Mark the cluster proposal as obsolete or remap it to the live registry | `docs/validation_mapping_table.md:150-183`, `docs/validation_mapping_table.md:408-409`, `debate/debate-index.md:28-31` |

## System Health

- Blocking counts: `0 [BLOCK-DEBATE]`, `0 [BLOCK-DRAFT]`, `0 [BLOCK-PUBLISH]`, `3 [WARNING]`, `1 [NOTE]`.
- Drift hotspots: authoritative wave prose around `016`; stale workflow ledgers in `PLAN.md` / `EXECUTION_PLAN.md`; pre-debate evidence hygiene in future-wave dossiers.
- Wave readiness: Wave 1 is complete. Wave 2 is operationally unblocked, but `016` remains Wave 2.5 only, and `003` / `014` remain downstream-gated per `debate-index.md`.

## Coverage Note

- Deliberately not verified: correctness of x38's interpretation of x37 content; only x38-side citation hygiene and authority usage were audited.
- Deliberately not verified: every extra-archive reference in every topic file. I ran a targeted sweep on the dossiers most relevant to upcoming wave transitions (`003`, `010`, `014`, `016`) and on the active closure/state artifacts.
- Draft-seeding was tested but not published as a governance finding because authority text is mixed: `drafts/README.md` is stricter than the higher-authority workflow rules.
