# X37 Results Index

Canonical session outputs nằm trong chính từng `sessions/sNN_*/`.
Final deliverables của session nằm trong `sessions/sNN_*/verdict/`.
Cross-session comparison nằm trong `analysis/results/`.

Thư mục này chỉ chứa index, không chứa canonical outputs.
