"""X37 shared infrastructure.

This package starts nearly empty. Sessions build shared modules incrementally
as they need reusable primitives. See x37_RULES.md §12 for allowed categories
and naming conventions.

Current modules: (none yet — first session will populate)
"""
